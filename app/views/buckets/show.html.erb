<p id= "notice"><%= notice %></p>

<p>
   <strong>Title:</strong>
   <%= @bucket.title %>
</p>

<p>
   <strong>Body:<strong>
   <%= @bucket.body %>
</p>

<%= link_to 'Edit', edit_bucket_path(@bucket) %>
<%= link_to 'Back', bucket_path %>