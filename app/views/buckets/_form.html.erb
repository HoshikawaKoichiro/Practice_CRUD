<%= form_with(model: bucket) do |form| %>
  <% if bucket.errors.any? %>
    <div id= "error_explanation" >
      <h2><%= pluralize(bucket.errors.count, "error") %>prohibited this bucket form being saved: </h2>

      <ul>
        <%= bucket.errors.each do |error| %>
        <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class= "field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class= "field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class= "actions">
    <%= form.submit %>
  </div>

<% end %>