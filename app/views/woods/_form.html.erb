<%= form_with(model:wood) do |form| %>
  <% if wood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wood.errors.count,"error") %>prohibited this wood from being saved:</h2>

      <ul>
        <% wood.errors.each do |error| %>
        <li><%= error.full_message %></li>
        <% end %>
      </li>
    </div>
  <% end %>

    <div class = "field" >
        <%= form.label :title %>
        <%= form.text_field :title %>
    </div>
    <div class="field">
      <%= form.label :body%>
      <%= form.text_area :body %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>