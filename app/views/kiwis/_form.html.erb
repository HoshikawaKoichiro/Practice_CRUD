<%= form_with(model:kiwi) do |form| %>
  <% if kiwi.errors.any? %>
    <div id="error_explanaton">
      <h2><%= pluralize(kiwi.errors.count, "error") %>prohibited this kiwi from being saved:</h2>

      <ul>
        <% kiwi.errors.each do |error| %>
        <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class ="field">
      <%= form.label :title%>
      <%= form.text_field :title%>
    </div>

    <div class="field">
      <%= form.label :body%>
      <%= form.text_area :body%>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>